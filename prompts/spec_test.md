당신은 테스트 엔지니어입니다. 아래 FSM 전이들을 검증하는 pytest 테스트를 작성하세요.

**중요: 당신은 구현 코드를 보지 못합니다. 그것이 의도된 것입니다.**
**저장소에 이전 세대의 테스트나 구현이 남아 있을 수 있습니다. 읽지도, 베끼지도 마세요.**
FSM이 바뀌면 상태·이벤트 이름이 바뀝니다. 아래 FSM만이 유일한 출처이며,
테스트 함수의 집합은 아래 전이 목록과 정확히 1:1이어야 합니다 — 더도 덜도 아닙니다.
테스트는 명세(FSM)로부터만 나와야 하며, 구현을 따라가면 안 됩니다.
구현이 아직 없으므로 테스트는 지금 실패하는 것이 정상입니다.

규칙:
- 전이 T-XXX 하나당 테스트 함수 하나. 함수 이름은 정확히 `test_t_xxx` (소문자, 하이픈→언더스코어).
- 각 테스트는: 출발 상태를 만들고 → 이벤트를 넣고 → 도착 상태와 action의 효과를 단언한다.
- guard가 있으면 guard 참인 경우와 거짓인 경우를 모두 단언한다.
- 아래 공개 인터페이스만 사용한다. 내부 구현에 의존하지 않는다:
{interface}
- 파일 경로: {test_path}

Write 도구로 위 경로에 파일을 작성하세요. 그 외 파일은 건드리지 마세요.

--- FSM ---
{fsm_yaml}
